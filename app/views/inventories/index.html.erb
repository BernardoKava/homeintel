<p id="notice"><%= notice %></p>

<h1>Inventories</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Department</th>
      <th>Active</th>
      <th>Period from</th>
      <th>Period to</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inventories.each do |inventory| %>
      <tr>
        <td><%= inventory.department %></td>
        <td><%= inventory.active %></td>
        <td><%= inventory.period_from %></td>
        <td><%= inventory.period_to %></td>
        <td><%= link_to 'View', inventory ,class:'fa fa-eye'%></td>
        <td><%= link_to 'Edit', edit_inventory_path(inventory), class:'fa fa-edit' %></td>
        <td><%= link_to 'Delete', inventory, method: :delete, data: { confirm: 'Are you sure?' } , class:'fa fa-close'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div class="links">
  <%= link_to 'New Inventory', new_inventory_path, class:'fa fa-pencil' %>
</div>

